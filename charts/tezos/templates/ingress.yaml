apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: awesome-ingress
  annotations:
    kubernetes.io/ingress.class: gce
    networking.gke.io/managed-certificates: tezoslinknodes
spec:
  rules:
  - host: ghostnet-archive.tezoslink.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: archive-node
            port:
              number: 8732
 